apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Use the Docker service name to access Prometheus
    url: http://simulation-prometheus:9090
    uid: prometheus  # Set fixed UID that dashboard references
    isDefault: true
    editable: true
    jsonData:
      timeInterval: '15s'
